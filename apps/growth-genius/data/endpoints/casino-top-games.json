{
	"name": "Casino Top Games",
	"description": "Fortunes Veil top games analytics endpoint.",
	"url": "https://api.fortunesveil.com/casino/top-games",
	"method": "POST",
	"apiKeyEnv": "ENDPOINT_SECRET",
	"body": {},
	"aiParameters": {
		"prompt": "Map the user's analytics request to the most relevant top-games filters. Only set a field when the request clearly indicates it.",
		"fields": {
			"timeframe": {
				"type": "enum",
				"options": ["today", "yesterday", "7d", "30d", "90d"],
				"description": "Choose the requested time range for top games.",
				"resolutionMode": "deterministic-only",
				"analyticsRangeMapping": {
					"anchor": "today",
					"exactDayOptions": {
						"today": "today",
						"yesterday": "yesterday"
					},
					"spanDayOptions": {
						"7": "7d",
						"30": "30d",
						"90": "90d"
					}
				}
			},
			"sortBy": {
				"type": "enum",
				"options": ["bets", "wins", "plays"],
				"description": "Choose the metric the user seems to care about for ranking top games."
			}
		}
	}
}